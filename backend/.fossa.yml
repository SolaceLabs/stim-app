version: 3

project:
  locator: SolaceLabs_stim-app-backend
  id: SolaceLabs_stim-app-backend
  name: stim-app-backend
  teams: []
  labels:
    - python

vendoredDependencies:
  forceRescans: false
  scanMethod: CLILicenseScan
  licenseScanPathFilters:
    exclude:
      - "./.git"
      - "./.github"

paths:
  exclude:
    - ./.git
    - ./.github

telemetry:
  scope: full
